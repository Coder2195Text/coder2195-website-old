{
    "$schema": "http://json-schema.org/schema",
    "required": [
        "homeTitle"
    ],
    "properties": {
        "homeTitle": {
            "type": "string",
            "description": "title on home page"
        }
    }
}